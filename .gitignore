# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Raw datasets (large; regenerate the processed file with data_pipeline.py)
raw_data/

# OS / editor cruft
.DS_Store
*~
*.swp
~$*

# Office lock/temp files
~$*.pptx
~$*.docx

# Presentation deliverables (kept local, not part of the code repo)
*.pptx
hack_script.txt
build_pptx.py
build_hack_pptx.py

# Local tooling state (not part of the project)
.remember/
.claude/

# Secrets / credentials — never commit
*.env
.env
*credentials*.csv
*accessKeys*.csv
*api-key*.csv
CLAUDE.md

# Hackathon pitch/prep notes (kept local)
DATA_AND_SATELLITE_EXPLAINED.md
DEMO_SCRIPT.md
PROJECT_EXPLAINER.md
